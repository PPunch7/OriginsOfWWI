<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.originsofworldwari">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/FullScreen">
        <activity android:name=".InformationActivity" />
        <activity
            android:name=".DiplomaticAttack"
            android:theme="@style/PopupControl" />
        <activity
            android:name=".NationInfoActivity"
            android:theme="@style/PopupControl" />
        <activity
            android:name=".OpportunityAttackActivity"
            android:theme="@style/PopupControl" />
        <activity
            android:name=".AiActionsActivity"
            android:theme="@style/PopupControl" />
        <activity
            android:name=".AttackActivity"
            android:theme="@style/PopupControl" />
        <activity
            android:name=".ActionActivity"
            android:theme="@style/PopupControl" />
        <activity
            android:name=".EndGameActivity"
            android:theme="@style/PopupControl" />
        <activity android:name=".GameActivity" />
        <activity android:name=".SelectNationsActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>